#include <bits/stdc++.h>
using namespace std;
struct ListNode {
    int val;
    ListNode *next;
};
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        // ListNode * durgesh = new ListNode(1);
        // durgesh->next = head;
        // ListNode* a = durgesh;
        // ListNode* b = durgesh;S
        // while(n--)
        // {
        //     a = a->next;
        // }
        // while(a->next != NULL)
        // {
        //     a= a->next;
        //     b = b->next;
        // }
        // b->next = b->next->next;
        // return durgesh->next;
    }
};
int main()
{
    
    return 0;
}